// Q : using system at once like #pragma once ? 
// A : there's no like #pragma once, because cs have no header file
using System;

namespace Main
{
    internal class Program
    {
        public static void Main()
        {
            return;
        }
    }
}